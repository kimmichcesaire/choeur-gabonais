{
  "name": "choeur-gabonais",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/backend",
    "packages/shared"
  ],
  "scripts": {
    "dev":      "concurrently -n frontend,backend -c green,blue \"npm run dev -w apps/frontend\" \"npm run dev -w apps/backend\"",
    "frontend": "npm run dev -w apps/frontend",
    "backend":  "npm run dev -w apps/backend",
    "build":    "npm run build -w packages/shared && npm run build -w apps/frontend && npm run build -w apps/backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
